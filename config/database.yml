default: &default
  encoding: unicode
  adapter: postgis
  pool: 5
  timeout: 5000
  username: postgres

development:
  <<: *default
  host: "<%= ENV['POSTGRES_HOST'] || 'protectedplanet-db' %>"
  username: "<%= ENV['POSTGRES_USER'] %>"
  password: "<%= ENV['POSTGRES_PASSWORD'] %>"
  database: "<%= ENV['POSTGRES_DBNAME'] %>"

test:
  <<: *default
  host: "<%= ENV['POSTGRES_HOST'] || 'protectedplanet-db' %>"
  username: "<%= ENV['POSTGRES_USER'] %>"
  password: "<%= ENV['POSTGRES_PASSWORD'] %>"
  database: "<%= ENV['POSTGRES_TEST_DBNAME'] %>"

production:
  <<: *default
  database: pp_production

staging:
  <<: *default
  database: pp_staging
